{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "written_post@1",
  "title": "A finished post",
  "description": "What the writing call returns. The text is what would appear on Facebook, not a draft of it.",
  "type": "object",
  "required": [
    "text",
    "picture_hint",
    "content_type",
    "leads_to_offer"
  ],
  "additionalProperties": false,
  "properties": {
    "text": {
      "type": "string",
      "minLength": 40,
      "maxLength": 3000,
      "description": "The post as published. First line is the hook, because Facebook truncates the rest."
    },
    "picture_hint": {
      "type": "string",
      "minLength": 5,
      "maxLength": 300,
      "description": "One Vietnamese line for the owner: what picture would suit this post. Nothing is generated from it."
    },
    "content_type": {
      "type": "string",
      "enum": [
        "observation",
        "story",
        "education",
        "offer"
      ]
    },
    "leads_to_offer": {
      "type": "string",
      "minLength": 3,
      "description": "The offer this post moves the reader toward, even if that offer is weeks away. The strategy's third check."
    }
  }
}
